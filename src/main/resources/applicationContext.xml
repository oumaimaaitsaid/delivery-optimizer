<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           https://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/data/jpa
           https://www.springframework.org/schema/data/jpa/spring-jpa.xsd
           http://www.springframework.org/schema/context
           https://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Scanne les composants (controllers, services, repositories) -->
    <context:component-scan base-package="com.delivery.optimizer"/>

    <!-- Source de données H2 en mode fichier -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="org.h2.Driver"/>
        <property name="url" value="jdbc:h2:file:./data/deliveriesdb;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1"/>
        <property name="username" value="sa"/>
        <property name="password" value=""/>
    </bean>

    <!-- EntityManagerFactory pour JPA/Hibernate -->
    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="packagesToScan" value="com.delivery.optimizer.model"/>
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.H2Dialect</prop>
                <prop key="hibernate.show_sql">true</prop>
                <prop key="hibernate.format_sql">true</prop>
                <prop key="hibernate.hbm2ddl.auto">update</prop>
                
            </props>
        </property>
    </bean>

    <!-- Transaction manager -->
    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory"/>
    </bean>

    <!-- Beans utilitaires -->
    <bean id="distanceCalculator" class="com.delivery.optimizer.util.DistanceCalculator"/>
    <bean id="nearestNeighborOptimizer" class="com.delivery.optimizer.optimizer.NearestNeighborOptimizer"/>
    <bean id="clarkeWrightOptimizer" class="com.delivery.optimizer.optimizer.ClarkeWrightOptimizer">
    <constructor-arg ref="distanceCalculator"/>
</bean>

    <!-- TourService -->
    <bean id="tourService" class="com.delivery.optimizer.service.TourService">
        <constructor-arg index="0" ref="nearestNeighborOptimizer"/>
        <constructor-arg index="1" ref="distanceCalculator"/>
    </bean>

</beans>
